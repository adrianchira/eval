<%= form_for(@evalist) do |f| %>
  <% if @evalist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evalist.errors.count, "error") %> prohibited this evalist from being saved:</h2>

      <ul>
      <% @evalist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :evaluator_id %><br />
    <%= f.number_field :evaluator_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
